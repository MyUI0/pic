name: Update Subscription Feed with Bark Notifications

on:
  schedule:
    - cron: '0 0 */2 * *'
  workflow_dispatch:
    inputs:
      send_test:
        description: '发送测试通知'
        required: false
        default: false
        type: boolean
  push:
    branches: [ main ]

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4

    - name: 🔔 发送开始通知
      if: ${{ github.event.inputs.send_test != 'true' }}
      id: notify-start
      run: |
        echo "🔔 发送工作流开始通知..."
        if [ -z "${{ secrets.BARK_DEVICE_KEY }}" ]; then
          echo "⚠️ BARK_DEVICE_KEY未设置，跳过Bark通知"
          echo "skip_bark=true" >> $GITHUB_OUTPUT
        else
          echo "✅ Bark设备密钥已配置"
          echo "skip_bark=false" >> $GITHUB_OUTPUT
          TITLE="🔄 订阅更新开始"
          BODY="开始订阅更新任务\n\n仓库: ${{ github.repository }}\n时间: $(date -u +'%Y-%m-%d %H:%M:%S UTC')\n触发方式: ${{ github.event_name }}"
          curl -s -X GET \
            "https://api.day.app/${{ secrets.BARK_DEVICE_KEY }}/$TITLE/$BODY?group=subscription-update&sound=bell&isArchive=1" \
            --max-time 10 || echo "⚠️ 开始通知发送失败"
        fi

    - name: 🔔 发送测试通知（手动触发）
      if: ${{ github.event.inputs.send_test == 'true' }}
      run: |
        echo "🧪 发送测试通知..."
        if [ -z "${{ secrets.BARK_DEVICE_KEY }}" ]; then
          echo "❌ 错误: BARK_DEVICE_KEY未设置"
          echo "请在GitHub仓库Secrets中设置BARK_DEVICE_KEY"
          exit 1
        fi
        TITLE="🔔 Bark测试通知"
        BODY="这是一个测试通知，用于验证Bark配置。\n\n仓库: ${{ github.repository }}\n时间: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        curl -s -X GET \
          "https://api.day.app/${{ secrets.BARK_DEVICE_KEY }}/$TITLE/$BODY?group=test&sound=bell&badge=1" \
          --max-time 10
        echo "✅ 测试通知已发送"
        echo "请检查您的iPhone是否收到通知"

    - name: 🔄 解析和更新订阅源
      id: update-feeds
      # 调用外部脚本执行核心逻辑
      run: bash ./scripts/update-feeds.sh

    - name: 💾 提交更改
      id: commit-changes
      if: always()
      run: |
        echo "检查文件更改..."
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        HAS_CHANGES=false
        if git diff --quiet feeds/; then
          echo "📭 未检测到文件更改"
          echo "has_changes=false" >> $GITHUB_OUTPUT
        else
          echo "📝 检测到更改，正在提交..."
          git add feeds/
          COMMIT_TIME=$(date -u +'%Y-%m-%d %H:%M:%S UTC')
          git commit -m "🔧 自动更新订阅源 [$COMMIT_TIME]

          • V2Ray订阅: ${{ steps.update-feeds.outputs.v2ray_count }} 行
          • Clash订阅: ${{ steps.update-feeds.outputs.clash_count }} 行
          • 更新状态: ${{ steps.update-feeds.outputs.update_status }}

          由GitHub Actions自动执行"
          if git push origin main; then
            echo "✅ 更改提交成功"
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "❌ 提交失败"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi
        fi

    - name: 🚀 部署到GitHub Pages
      if: steps.commit-changes.outputs.has_changes == 'true'
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./feeds
        publish_branch: gh-pages
        keep_files: true

    - name: 🔔 发送成功通知
      if: steps.update-feeds.outputs.update_status == 'success' && steps.commit-changes.outputs.has_changes == 'true' && steps.notify-start.outputs.skip_bark == 'false'
      run: |
        echo "✅ 发送成功通知..."
        TITLE="✅ 订阅更新成功"
        BODY="订阅源已成功更新并部署！\n\n📦 仓库: ${{ github.repository }}\n🕐 时间: $(date -u +'%Y-%m-%d %H:%M:%S UTC')\n📊 统计信息:\n• V2Ray: ${{ steps.update-feeds.outputs.v2ray_count }} 行\n• Clash: ${{ steps.update-feeds.outputs.clash_count }} 行\n\n🔗 访问URL:\nhttps://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
        curl -s -X GET \
          "https://api.day.app/${{ secrets.BARK_DEVICE_KEY }}/$TITLE/$BODY?group=subscription-update&sound=paymentsuccess&badge=1&isArchive=1&url=https://kkgithub.com/${{ github.repository }}/actions" \
          --max-time 10 || echo "⚠️ 成功通知发送失败"

    - name: 🔔 发送部分失败通知
      if: steps.update-feeds.outputs.update_status == 'partial_failure' && steps.commit-changes.outputs.has_changes == 'true' && steps.notify-start.outputs.skip_bark == 'false'
      run: |
        echo "⚠️ 发送部分失败通知..."
        TITLE="⚠️ 订阅部分更新"
        BODY="订阅源部分更新！\n\n📦 仓库: ${{ github.repository }}\n🕐 时间: $(date -u +'%Y-%m-%d %H:%M:%S UTC')\n📊 统计信息:\n• V2Ray: ${{ steps.update-feeds.outputs.v2ray_count }} 行\n• Clash: ${{ steps.update-feeds.outputs.clash_count }} 行\n\n❌ 错误信息:\n${{ steps.update-feeds.outputs.error_msg }}\n\n🔗 访问URL:\nhttps://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
        curl -s -X GET \
          "https://api.day.app/${{ secrets.BARK_DEVICE_KEY }}/$TITLE/$BODY?group=subscription-update&sound=alarm&badge=2&isArchive=1&url=https://kkgithub.com/${{ github.repository }}/actions" \
          --max-time 10 || echo "⚠️ 部分失败通知发送失败"

    - name: 🔔 发送无变化通知
      if: steps.update-feeds.outputs.update_status == 'success' && steps.commit-changes.outputs.has_changes == 'false' && steps.notify-start.outputs.skip_bark == 'false'
      run: |
        echo "📭 发送无变化通知..."
        TITLE="📭 无订阅更新"
        BODY="订阅检查完成，但未发现新更新。\n\n📦 仓库: ${{ github.repository }}\n🕐 时间: $(date -u +'%Y-%m-%d %H:%M:%S UTC')\n📊 当前状态:\n• V2Ray: ${{ steps.update-feeds.outputs.v2ray_count }} 行\n• Clash: ${{ steps.update-feeds.outputs.clash_count }} 行\n\n所有订阅源都是最新的。"
        curl -s -X GET \
          "https://api.day.app/${{ secrets.BARK_DEVICE_KEY }}/$TITLE/$BODY?group=subscription-update&sound=default&isArchive=1" \
          --max-time 10 || echo "⚠️ 无变化通知发送失败"

    - name: 🔔 发送完全失败通知
      if: steps.update-feeds.outputs.update_status == 'failed' && steps.notify-start.outputs.skip_bark == 'false'
      run: |
        echo "❌ 发送完全失败通知..."
        TITLE="❌ 订阅更新失败"
        BODY="订阅更新完全失败！\n\n📦 仓库: ${{ github.repository }}\n🕐 时间: $(date -u +'%Y-%m-%d %H:%M:%S UTC')\n❌ 错误信息:\n${{ steps.update-feeds.outputs.error_msg }}\n\n🔧 请检查:\n1. 源项目README是否可访问？\n2. 网络连接是否正常？\n3. GitHub Actions配置是否正确？"
        curl -s -X GET \
          "https://api.day.app/${{ secrets.BARK_DEVICE_KEY }}/$TITLE/$BODY?group=subscription-update&sound=alarm&level=critical&badge=99&isArchive=1&url=https://kkgithub.com/${{ github.repository }}/actions" \
          --max-time 10 || echo "⚠️ 失败通知发送失败"

    - name: 📊 工作流摘要
      if: always()
      run: |
        echo "=== 工作流执行摘要 ==="
        echo "仓库: ${{ github.repository }}"
        echo "触发方式: ${{ github.event_name }}"
        echo "开始时间: ${{ steps.notify-start.outputs.start_time || 'N/A' }}"
        echo "结束时间: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo ""
        echo "📊 更新统计信息:"
        echo "• V2Ray订阅: ${{ steps.update-feeds.outputs.v2ray_count || 0 }} 行"
        echo "• Clash订阅: ${{ steps.update-feeds.outputs.clash_count || 0 }} 行"
        echo "• 更新状态: ${{ steps.update-feeds.outputs.update_status || 'unknown' }}"
        echo ""
        echo "💾 文件更改: ${{ steps.commit-changes.outputs.has_changes || 'false' }}"
        echo "🚀 Pages部署: ${{ steps.commit-changes.outputs.has_changes == 'true' && '已触发' || '未触发' }}"
        echo ""
        echo "🔔 Bark通知:"
        echo "• 设备密钥: ${{ secrets.BARK_DEVICE_KEY && '已配置' || '未配置' }}"
        echo "• 跳过通知: ${{ steps.notify-start.outputs.skip_bark || 'false' }}"
        echo ""
        echo "🔗 您的永久订阅链接:"
        echo "V2Ray: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/v2ray-latest.txt"
        echo "Clash: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/clash-latest.yaml"
        echo "状态页面: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
        echo "====================="
